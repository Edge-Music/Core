# OMSP 音乐播放器 3.0 设计规范文档

> 版本：v3.0.0-draft
> 基于：2.5.1 → 3.0（Breaking Change）
> 分支：`feature/3.0`
> 平台：HarmonyOS / ArkTS / ArkUI
> 定位：基于 OMSP（Open Media Source Protocol，自定义标准数据源协议）的多数据源音乐播放器

---

## 0. 3.0 核心变更总览

| 项 | 2.5.1 | 3.0 |
|---|---|---|
| 首屏 | 发现页 | **播放页（沉浸式歌曲卡片）** |
| 发现页 | 独立 Tab | **取消**，能力下沉为「侧边栏 · 榜单与推荐」抽屉 |
| 设置页 | 独立入口 | 拆分为 **「我的」（数据源+歌单聚合）** + **「设置」（二级页）** |
| 视觉语言 | 无统一 Token / 扁平卡片 | **沉浸 · 光感 · 液态玻璃**，深浅双主题 Token 体系 |
| 内容浏览方式 | 列表/歌单进入 | **左右横滑卡片流**（类波点音乐/汽水音乐，但为横向） |
| 推荐 | 无 | **模式化推荐引擎**：新鲜 / 熟悉 / 默认，基于多数据源信号加权 |
| 数据源 | 单一/隐式 | **一等公民**：可添加、可登录多个、可在首页头像处直接切换 |

设计哲学一句话总结：
**「不是打开一个音乐 App 去找歌，而是打开就在听歌，边听边刷」。**

---

## 1. 设计哲学：沉浸 · 光感 · 液态玻璃

### 1.1 沉浸（Immersive）
- 首屏即播放页，专辑封面/MV 背景铺满全屏，作为整个界面的"环境"，而非一张卡片里的小图。
- 所有浮层（播控条、侧边栏、歌词）都以半透明玻璃材质悬浮在这个环境之上，而不是切换到新的纯色页面。
- 页面切换尽量使用**叠加/抽屉**而非路由跳转的生硬感（侧边栏、我的页可用 Sheet/Modal 呈现，减少"离开当前歌曲"的心理感受）。

### 1.2 光感（Ambient Light）
- 从当前播放歌曲封面中提取主色/辅色（取色算法见 §7.5），实时驱动：
  - 背景渐变色
  - 播控条按钮的高光色
  - 侧边栏/我的页打开时的氛围光晕
- 光感是**动态**的：歌曲切换时颜色以 400–600ms 缓动过渡，避免跳变。
- 深色环境下光感表现为"发光"（glow），浅色环境下表现为"渲染"（tint wash），见 §2 双主题定义。

### 1.3 液态玻璃（Liquid Glass）
- 所有悬浮层级（播控条容器、侧边栏、卡片切换指示器、Toast）使用统一的玻璃材质规范（模糊 + 半透明 + 边缘高光 + 微噪点）。
- 玻璃材质**不是静态的毛玻璃**，需要有"液态"感：跟随手指拖拽/歌曲切换时有轻微的折射变形或高光扫过（HarmonyOS 可用 `blur()` + 渐变蒙层 + `linearGradient` 边框模拟，进阶可用自定义 Shader）。
- 玻璃卡片边界统一使用 1px 渐变描边（顶部亮、底部暗），模拟真实玻璃的光线折射。

---

## 2. 色彩与视觉 Token 体系

采用「语义化三层 Token」：**原子色板 → 语义 Token → 组件 Token**，深浅主题各一套，且都要支持"光感覆盖层"（Ambient Overlay，运行时由封面取色动态覆盖语义 Token 中的 accent 系列）。

### 2.1 原子色板（Palette，与主题无关）

```
--omsp-neutral-0   #000000
--omsp-neutral-10  #0A0A0D
--omsp-neutral-20  #16161B
--omsp-neutral-30  #232329
--omsp-neutral-40  #38383F
--omsp-neutral-60  #6E6E76
--omsp-neutral-80  #B4B4BA
--omsp-neutral-95  #ECECEF
--omsp-neutral-100 #FFFFFF

--omsp-brand-500   #6E5BFF   /* 品牌主色：介于电光紫与靛蓝之间，兼容深浅主题的高光表现 */
--omsp-brand-300   #A79BFF
--omsp-brand-700   #4A3BC4

--omsp-accent-warm #FF8A5C   /* 光感暖色候选（取色回退色之一） */
--omsp-accent-cool #4CC9F0   /* 光感冷色候选 */

--omsp-danger      #FF5A5F
--omsp-success     #35C97B
--omsp-warning     #FFB020
```

### 2.2 深色主题语义 Token（Dark · 默认，液态玻璃表现最强）

| Token | 值/说明 |
|---|---|
| `--bg-canvas` | 由封面取色生成的深色渐变（兜底 `neutral-10 → neutral-20`）|
| `--surface-glass-1` | `rgba(255,255,255,0.08)` + blur(28) — 播控条/侧边栏主容器 |
| `--surface-glass-2` | `rgba(255,255,255,0.14)` + blur(20) — 卡片内二级浮层（如歌单弹层） |
| `--glass-border` | 渐变 `rgba(255,255,255,0.35) → rgba(255,255,255,0.05)`（顶亮底暗） |
| `--glass-highlight` | `rgba(255,255,255,0.5)`，用于滑动时的高光扫过动效 |
| `--text-primary` | `neutral-100` |
| `--text-secondary` | `rgba(255,255,255,0.64)` |
| `--text-tertiary` | `rgba(255,255,255,0.38)` |
| `--accent-ambient` | 运行时由封面取色注入，兜底 `brand-500` |
| `--control-active` | `--accent-ambient`，用于播放按钮/进度条前景 |
| `--control-track` | `rgba(255,255,255,0.2)`，进度条底轨 |
| `--divider` | `rgba(255,255,255,0.08)` |
| `--shadow-ambient` | `0 8px 40px rgba(0,0,0,0.45)`，玻璃层投影，营造悬浮感 |

### 2.3 浅色主题语义 Token（Light · 光感通透）

| Token | 值/说明 |
|---|---|
| `--bg-canvas` | 由封面取色生成的浅色渐变（兜底 `neutral-95 → neutral-100`），饱和度降低、亮度提高，避免刺眼 |
| `--surface-glass-1` | `rgba(255,255,255,0.55)` + blur(28) |
| `--surface-glass-2` | `rgba(255,255,255,0.7)` + blur(20) |
| `--glass-border` | 渐变 `rgba(255,255,255,0.9) → rgba(255,255,255,0.2)` |
| `--glass-highlight` | `rgba(255,255,255,0.85)` |
| `--text-primary` | `neutral-10` |
| `--text-secondary` | `rgba(10,10,13,0.6)` |
| `--text-tertiary` | `rgba(10,10,13,0.36)` |
| `--accent-ambient` | 运行时注入，兜底 `brand-700`（浅色下用更深的品牌色保证对比度） |
| `--control-active` | `--accent-ambient` |
| `--control-track` | `rgba(10,10,13,0.12)` |
| `--divider` | `rgba(10,10,13,0.08)` |
| `--shadow-ambient` | `0 8px 32px rgba(0,0,0,0.12)` |

> **对比度规则**：`--accent-ambient` 注入前必须做 WCAG AA 校验（文字场景 ≥4.5:1，控件场景 ≥3:1），不达标则对取色结果做明度/饱和度修正，而不是放弃光感。

### 2.4 材质 Token（跨主题通用数值，仅表现随主题变化）

| Token | 说明 |
|---|---|
| `--radius-card` | 28vp — 主播放卡片圆角 |
| `--radius-sheet` | 32vp（顶部）— 抽屉/弹层圆角 |
| `--radius-control` | 24vp — 播控条圆角（近似胶囊） |
| `--radius-chip` | 16vp — 榜单/模式 Chip |
| `--blur-1` | 28vp（主浮层） |
| `--blur-2` | 44vp（背景环境层，配合封面做强模糊） |
| `--motion-standard` | 320ms, cubic-bezier(0.25,0.8,0.25,1) |
| `--motion-liquid` | 480ms, spring(damping:0.8) — 液态形变专用 |
| `--space-xs/s/m/l/xl` | 4/8/16/24/32 vp |

### 2.5 字体 Token
- 主标题（歌曲名）：HarmonyOS Sans / Medium 22–26sp
- 副标题（歌手）：Regular 15sp，`--text-secondary`
- 歌词高亮：Medium 20sp，`--text-primary` + 轻微发光阴影（深色）/加粗（浅色）
- 数字类（榜单排名、播放量）：等宽数字特性 `fontFeature('tnum')`

---

## 3. 信息架构 / 页面地图

```
启动
 └─ 首页【播放页】(Tab 根视图，无底部 Tab 栏，全屏沉浸)
     ├─ 左滑手势 → 侧边栏抽屉【模式 + 榜单/推荐】
     ├─ 右滑手势 → 上一首卡片 / 左滑 → 下一首卡片（横向卡片流）
     ├─ 上滑播控条 → 全屏歌词页
     ├─ 头像/数据源角标点击 → 数据源快速切换气泡
     └─ 底部图标 → 「我的」半屏/全屏页
             ├─ 顶部：当前数据源卡片（可展开多数据源列表 + 添加数据源）
             ├─ 聚合歌单区（固定：最近播放/我的收藏/本地下载 等，跨数据源聚合）
             ├─ 当前数据源的用户歌单列表
             └─ 右上角「设置」入口 → 设置页（二级路由页面，非叠加）
                    ├─ 通用设置
                    ├─ 播放与音质
                    ├─ 推荐与隐私（模式权重可视化、数据授权）
                    ├─ 数据源管理（增删改、OMSP 接口调试）
                    └─ 关于/版本(2.5.1→3.0 变更说明)
```

设计取舍说明：
- **首页不设底部 TabBar**，仅保留极简悬浮入口（侧边栏拉手 + 头像 + 我的入口），最大化沉浸感，避免和波点/汽水音乐同质化的同时保留其核心心智。
- 「我的」页面定位为**半模态卡片页**（从底部升起，非独立 Tab 根），符合"永远没有真正离开当前播放"的原则；「设置」才是真正的二级页面（因为信息密度高，需要完整可返回的导航栏）。

---

## 4. 首页 · 播放页详细设计

### 4.1 卡片流交互
- 主体是**横向分页卡片流**（类比 Gallery/Swiper，非上下滑），一张卡片 = 一首歌的沉浸展示。
- 卡片来源由当前「模式」决定（默认模式 = 播放队列顺序；新鲜/熟悉模式 = 推荐引擎实时生成的卡片流，见 §7）。
- 横滑到下一张 ≠ 强制切歌：需有"预览态"（半透明预告下一首封面）与"确认态"（滑动过阈值后才真正切歌并开始播放），避免误触。
- 每张卡片包含：封面/背景环境层、歌曲名+歌手（渐显）、评论/热评浮层（如参考图的弹幕式热评卡片，可选开关）、数据源来源角标（如"网易云""QQ音乐""NAS"小图标）。

### 4.2 播控条（悬浮玻璃组件，跨页面复用）
- 采用 `--surface-glass-1` + `--glass-border`，胶囊形态，固定于卡片底部安全区之上。
- 内容：进度条（`--control-active` 填充）、播放/暂停、上一首/下一首、更多操作（收藏、加入歌单、下载、分享）。
- 组件封装为 `OmspPlayerBar`，供设置页以外的**所有**页面复用（我的页展开歌单时依然可见的迷你播控条）。

### 4.3 侧边栏抽屉（模式 + 榜单）
- 从左侧滑出（或点击左上角按钮），玻璃材质全高抽屉，背景保留当前歌曲环境模糊。
- 顶部：**模式切换器**（新鲜模式 / 熟悉模式 / 默认模式），Segmented 控件，选中态用 `--accent-ambient` 高亮。
- 下方：当前数据源可提供的榜单/推荐列表（由 OMSP 数据源的 `charts` / `recommendations` 能力位暴露，若数据源不支持则该区域自动隐藏对应分组，保证多数据源兼容）。
- 每个榜单项点击后 = 替换当前卡片流的数据源（队列切换有明确 Toast 提示"已切换到：XX榜单"）。

### 4.4 数据源切换气泡
- 点击头像出现气泡列表：已登录数据源头像横排 + "＋添加数据源"，点击直接切换当前播放上下文的数据源（不打断当前播放，仅影响后续侧边栏内容与"我的"聚合）。

---

## 5. 「我的」页面详细设计

### 5.1 结构（自上而下）
1. **当前数据源卡片**：头像+名称+账号状态，点击展开为已登录数据源横向列表（可横滑），列表末尾"＋添加数据源"入口（进入数据源添加/OMSP 配置向导）。
2. **聚合歌单区（固定，跨数据源）**：
   - 最近播放（跨所有数据源合并去重，按时间排序）
   - 我的收藏（跨数据源合并）
   - 本地/离线下载
   - （可扩展）最近搜索
3. **当前数据源的用户歌单**：仅展示当前选中数据源下的歌单，随数据源切换气泡的选择实时刷新。
4. 右上角齿轮图标 → 设置页。

### 5.2 交互细节
- 页面本身以 Sheet 形式从底部升起，背景仍可见首页环境模糊（半透明玻璃，强调"没有离开播放"）。
- 数据源添加流程需支持：预置模板（网易云/QQ音乐/自建NAS）+ 自定义 OMSP Endpoint（URL + 鉴权方式：Cookie/Token/OAuth 三选一的向导式表单）。

---

## 6. 设置页详细设计

- 采用标准二级导航页（有 NavBar + 返回），因为信息密度高、需要精确操作，不适合玻璃悬浮层承载。
- 分组：通用 / 播放与音质 / 推荐与隐私 / 数据源管理 / 关于。
- 「推荐与隐私」需包含：
  - 三种模式的权重构成可视化（雷达图或堆叠条，让用户理解"熟悉模式=70%历史信号"这类构成，增强透明度与信任）。
  - 数据采集开关（是否允许基于歌词/播放行为做本地分析）。
- 「数据源管理」暴露 OMSP 调试信息（用于开发者/社区自测自定义数据源，如请求日志、字段映射校验结果）。

---

## 7. 推荐系统架构：新鲜 / 熟悉 / 默认模式

### 7.1 信号采集层（跨数据源聚合，OMSP 统一格式）
统一抽象为「行为信号」与「内容信号」两类：

**行为信号**（来自播放器本地埋点，独立于数据源）
- 播放次数 `playCount`
- 完整播放率 `completionRate`
- 跳过率 `skipRate`（3s/10s 内跳过判定为负反馈）
- 收藏/加入歌单动作 `collectAction`
- 最近播放时间衰减 `recencyDecay`

**内容信号**（来自各 OMSP 数据源的 `track/lyrics/playlist` 能力位聚合）
- 歌手/专辑/风格标签 `metaTags`
- 用户自建歌单内容构成 `userPlaylistComposition`
- 用户收藏歌单构成 `favoritePlaylistComposition`
- 歌词文本 → 轻量 NLP（本地词云/关键词提取，如 TF-IDF 或轻量 embedding）→ `lyricTopicVector`
- 当前数据源榜单/推荐位 `sourceCharts`（作为探索池，而非直接信号）

### 7.2 词云/语义层设计要点
- 对用户「常听+收藏」歌曲的歌词做关键词与情感基调提取，生成一个「用户语义画像向量」`userTopicVector`。
- 候选曲库中未听过的歌曲，若其歌词/标签与 `userTopicVector` 相似度高 → 视为「新鲜但相关」的探索候选，用于新鲜模式。
- 该模型定位为**轻量本地/端侧模型**（如小型 TF-IDF + 余弦相似度即可满足 MVP，无需大模型；若未来引入 embedding 模型，应保持可插拔，通过统一 `SemanticProvider` 接口对接，不与具体模型强绑定）。

### 7.3 三种模式的评分公式

统一打分函数（对候选池中每首候选曲目打分，取 Top-N 组成卡片流）：

```
score = w1 * recency
      + w2 * frequency
      + w3 * affinity      (同歌手/同风格/同歌单共现)
      + w4 * novelty        (语义相关但未听过)
      + w5 * lyricAffinity  (歌词语义相似度)
      + w6 * chartBoost     (榜单热度加权)
      - w7 * skipPenalty
```

### 7.4 各模式权重构成（说明性设计，非最终数值，需产品/数据侧微调）

| 权重 | 默认模式 Default | 熟悉模式 Familiar | 新鲜模式 Fresh |
|---|---|---|---|
| w1 recency | 0.20 | 0.30 | 0.05 |
| w2 frequency | 0.15 | 0.30 | 0.05 |
| w3 affinity | 0.20 | 0.25 | 0.15 |
| w4 novelty | 0.15 | 0.02 | 0.40 |
| w5 lyricAffinity | 0.10 | 0.08 | 0.20 |
| w6 chartBoost | 0.15 | 0.03 | 0.10 |
| w7 skipPenalty | 0.15 | 0.20 | 0.05（新鲜模式容忍更高跳过率，鼓励探索） |

设计意图对照：
- **默认模式**：均衡，接近"传统队列+轻推荐"，作为无历史数据新用户的兜底体验。
- **熟悉模式**：高权重给 `recency` + `frequency` + `affinity`，本质是"你爱听的、你常听的"，`novelty` 压到极低，`skipPenalty` 高（一旦用户跳过类似内容，快速抑制同类召回）。
- **新鲜模式**：`novelty` + `lyricAffinity` 主导，召回来自「多数据源榜单交叉」与「语义扩展」的候选池，`skipPenalty` 权重低，允许更大胆的探索、允许一定的"踩雷率"。

### 7.5 封面取色（光感驱动）与推荐系统的关系
- 取色算法（如 K-means/主色聚类，端侧实现）产出的 `dominantColor/accentColor` 仅服务于视觉层（§2.3 `--accent-ambient`），**不参与**推荐打分，两者解耦，避免视觉需求污染推荐逻辑。

### 7.6 冷启动与数据源缺失处理
- 若某数据源不提供歌词/标签能力位，则对应信号项在该数据源的候选曲目上置为中性值（既不加分也不减分），保证不同数据源候选公平参与统一排序，不因能力缺失被系统性打压。
- 新用户无历史行为数据时，默认模式自动退化为"数据源榜单 + 少量语义占位"的兜底候选池。

---

## 8. 组件封装清单（需复用的核心组件）

| 组件 | 说明 |
|---|---|
| `OmspGlassSurface` | 统一液态玻璃容器（封装 blur/border/highlight/shadow Token） |
| `OmspPlayerBar` | 悬浮播控条，全局挂载于播放页/我的页 |
| `OmspTrackCard` | 横向卡片流的单张卡片（封面/背景/元信息/热评浮层） |
| `OmspModeSwitcher` | 侧边栏顶部三模式切换 Segmented 控件 |
| `OmspSourceAvatarBubble` | 头像数据源切换气泡 |
| `OmspAggregatedPlaylistSection` | 「我的」聚合歌单区块（固定分类+跨源合并逻辑） |
| `OmspSettingsGroup` | 设置页分组列表项，统一二级页风格 |
| `AmbientColorProvider` | 封面取色→主题 Token 覆盖的运行时 Provider（含 WCAG 校验兜底） |

---

## 9. 待确认事项（Open Questions，需在开发前逐一拍板）

1. 卡片流的"预览态→确认态"具体阈值（滑动百分比/速度）尚未定义，需交互侧给出手感规范。
2. 热评弹幕层是否为 MVP 必做项，或作为 3.1 增量。
3. 语义模型（TF-IDF vs 轻量 embedding）选型与端侧算力评估（尤其低端设备）需单独立项验证。
4. 数据源鉴权向导的三种方式（Cookie/Token/OAuth）在 OMSP 协议层的字段定义需要与协议文档同步更新（协议本身的 Breaking Change 需单独文档化，本设计文档只覆盖 UI/交互/推荐侧）。
5. 深浅主题下 `--accent-ambient` 的 WCAG 修正算法（明度/饱和度调整策略）需要具体实现方案评审。

---

## 10. 开发流程建议

1. 基于 `feature/3.0` 分支进行开发，视觉与组件层建议先落地 §2 Token 与 §8 组件清单（不依赖具体页面），再自顶向下接入各页面。
2. 页面替换顺序建议：先做首页播放页（影响面最大、决定沉浸感基调）→ 侧边栏 → 我的页 → 设置页 → 推荐引擎打分逻辑接入。
3. 每完成一个里程碑，建议使用 DevEco CLI 进行编译验证，并结合鸿蒙官方组件文档核对 ArkUI 能力边界（如模糊/材质相关 API 的版本兼容性），避免视觉设计超出目标 HarmonyOS 版本的实际渲染能力。
4. 建议将 §7 的权重表格做成可配置项（而非硬编码常量），便于后续 A/B 与数据侧调优。

---

*本文档为 3.0 视觉与交互设计规范，不包含 OMSP 协议字段级 Breaking Change 说明，后者应在协议文档中单独维护并保持版本对应关系。*
