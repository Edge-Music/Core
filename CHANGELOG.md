# 更新日志

本项目版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
`versionCode` 规则：`major × 10⁷ + minor × 1000 + patch × 100`，末两位留给热修版本。

---

## [3.1.0] - 2026-08-08

自 3.0.1 以来累计 39 次提交，主要围绕**桌面卡片**、**播放稳定性**、**多设备形态适配**三条线推进。

### ✨ 新增功能

**桌面卡片（感谢 @zitann）**
- 新增**动态歌词卡片**，歌词切换带滑动过渡动画
- 卡片封面加入立体效果与歌词光晕，整体背景质感优化
- 切歌按钮加入缩放动画反馈
- 移除已无实际作用的音乐推荐卡片

**播放与歌词（感谢 @jizhishi）**
- 接入**系统桌面悬浮歌词**（需 API ≥ 23）
- 新增**时间屏保**：大字时钟 + 玻璃播控卡，支持长按自定义时钟字体/字号、下滑退出、三分钟无操作自动压暗防烧屏
- 下载歌曲时自动将标题、封面、歌手、歌词写入 FLAC 文件；部分 MP3 格式亦支持封面写入
- 播放本地音乐时自动读取文件内嵌封面
- 应用颜色主题现已同步到系统层面的应用主题设置

**界面布局（感谢 @lsbnbdz、@jizhishi）**
- 电脑 / 自由多窗形态初步适配
- 侧边栏顶部新增设置入口，移除原音源页顶部的设置按钮
- 调整侧边栏布局，横屏下歌单同样可以翻动
- 「我的」面板在宽屏下改为响应式双栏

**播放行为**
- 播放界面菜单在歌曲已收藏时显示「取消收藏」（感谢 @zitann）
- 退出应用后再次进入保留歌曲信息（不含播放进度）（感谢 @lsbnbdz）

### 🎯 播放稳定性（本次重点）

针对快速切歌导致解码器异常的问题，重构了 Free-PCM 播放管线的并发控制：

- **修复快速连续切歌打崩解码器**。三个叠加的竞态：起播链的 Promise 未向上返回导致时效校验失效、序号抢占发生在 `await` 之后、过期请求未回收自己创建的解码器（每次快切泄漏一个持续运行的解码线程）。现已改为序号同步抢占 + 管线重建串行锁 + 过期/失败分支强制回收解码器
- **解码器关闭后等待解码线程真正退出**再创建新解码器，避免两个线程争用同一套底层资源
- **切歌入口统一防抖**：首次点击零延迟保持跟手，300 ms 内的连点只保留最后一次；自动续播不受影响、曲间无额外空白
- **修复单曲队列播完后卡死**：队列只有一首歌时播完不会循环、点播放键无反应、进度归零。根因是「歌曲 id 相同就只续播」的短路逻辑忽略了解码器已 EOS 这一事实
- **修复单曲循环下切歌按钮失效**：`playNext` 的强制切歌参数从未被任何调用点传入，导致单曲循环时点「下一首」只会原地重播
- **修复非强制音频打断时声音未真正停止**：原实现只同步了状态而未暂停 renderer，表现为界面显示已暂停但音频仍在播放
- **修复播放器监听器泄漏**：暂停/恢复一次就会重复注册一份 renderer 回调，累积后导致状态错乱与卡顿；
  播放条、屏保、歌词页、握持悬浮球等组件此前注册匿名回调却无法摘除（`removeListener` 按引用比对，
  传入新建的空函数永不匹配），组件每次重建都会累加一份并写入已失效的组件
- 移除播放完成看门狗中每 200 ms 一次的日志输出（单曲可产生上千条，明显拖慢 UI 线程）

### 🐛 问题修复

**桌面卡片（感谢 @zitann）**
- 修复无后台进程时，卡片需点击两次按钮才能播放
- 修复卡片、App、播控中心三端收藏状态不同步
- 修复卡片无法加载本地歌曲封面
- 修复卡片起播后打开 App 歌曲被暂停、清后台后卡片状态不同步
- 修复播放时新增卡片状态错误
- 修复音频被打断后卡片停留在播放状态卡死

**其他**
- 修复应用不在首页时，从播控中心进入会把首页移到栈顶、导致非首页被当作新首页（感谢 @jizhishi）
- 修复返回键逻辑：首页返回时优先关闭侧边栏（感谢 @lsbnbdz）
- **修复智感握持切后台再回前台后永久失效**：回到前台时误将握持监听注销，而重新注册
  只发生在首页首次挂载，导致启动后只能生效一次；监听注册同时加了幂等守卫，避免重复
  注册使一次握持触发多次悬浮球移动
- **修复歌词页横屏按钮有时点击无响应**：原先用 `getLastWindow` 取窗口，在悬浮歌词窗或
  多功能悬浮球窗存在时会拿到子窗口，对子窗口设置方向无效；改为显式操作主窗口，失败时
  给出提示而非静默忽略
- 修复屏保进度条无法拖动（定时器每 500 ms 把滑块拉回旧位置），并修好点击轨道跳转不生效的问题
- 修复无播放内容时屏保横屏胶囊显示字面量 `undefined`
- 屏保时钟字号滑杆改为拖动结束才写入偏好，消除拖动期间的持续磁盘 IO
- 修复进度条组件在多指操作下可能因空触点列表崩溃
- 移除播控中心上下曲的额外节流层（原会导致连点五次只跳一首）
- 清理智感握持回调中每 5 秒空转一次的无效定时器

### 💅 体验优化（感谢 @lsbnbdz）

- 差异化横竖屏歌词偏移：横屏 0.45，竖屏保持 0.25
- 去除横屏歌词底部避让，改为沉浸展示
- 调整歌词页底部进度条样式
- 顶部展开侧栏按钮颜色模式调整为 LIGHT，去掉按钮阴影

### 🙏 贡献者

本版本由以下开发者共同完成：

| 贡献者 | 提交数 | 主要贡献 |
| --- | --- | --- |
| [@zitann](https://github.com/Zitann) | 18 | 桌面卡片全线重做：动态歌词卡片、立体封面、状态同步与多项卡片缺陷修复 |
| @jizhishi | 13 | 系统悬浮歌词、时间屏保、FLAC/MP3 标签写入、自由多窗适配、播放卡顿优化 |
| @lsbnbdz | 8 | 侧边栏布局与设置入口、歌词页视觉打磨、返回键交互、歌曲信息保留 |
| [@Okysu](https://github.com/Okysu) | — | 播放管线并发重构与稳定性修复 |

---

## [3.0.1] - 2026-07-20

- 补回首页悬浮球握持手势监听
- 「我的」面板宽屏响应式双栏布局
- 推荐流式续播、收藏同步与队列自动补充

## [3.0.0] - 2026-07-20

- OMSP 3.0 沉浸式改版
